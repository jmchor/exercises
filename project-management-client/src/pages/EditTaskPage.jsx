import { useParams, useNavigate } from 'react-router-dom';
import { useState, useEffect } from 'react';
import axios from 'axios';

const EditTask = () => {
        // set state variables
        // use effect with axios GET to show edit form
        // handle submit with axios PUT to update the task
};

export default EditTask;

// needs GET PUT and DELETE routes in backend
